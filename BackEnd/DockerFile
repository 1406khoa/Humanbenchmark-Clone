# Sử dụng Node.js phiên bản 18 trên Alpine
FROM node:18-alpine

# Thiết lập thư mục làm việc trong container
WORKDIR /app/BackEnd

# Sao chép file package.json và package-lock.json để cài đặt dependencies
COPY package.json package-lock.json ./

# Cài đặt các package cần thiết (loại trừ dev dependencies)
RUN npm install --omit=dev

# Sao chép toàn bộ mã nguồn vào container
COPY . .

# Mở cổng 3000 (lưu ý: Railway sẽ override biến PORT nếu cần)
EXPOSE 3000

# Lệnh khởi động ứng dụng Node.js
CMD ["node", "server.js"]
